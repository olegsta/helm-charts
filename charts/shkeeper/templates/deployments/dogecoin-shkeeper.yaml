{{- if .Values.ltc.enabled}}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: dogecoin-shkeeper
  namespace: {{ .Values.namespace }}
  labels:
    app: dogecoin-shkeeper
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dogecoin-shkeeper
  template:
    metadata:
      labels:
        app: dogecoin-shkeeper
    spec:
      {{- with .Values.dev.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: app
          image: {{ .Values.dogecoin_shkeeper.image }}
          command:
          - gunicorn
          - --access-logfile=-
          - --workers=1
          - --threads=16
          - --timeout=600
          - --bind=0.0.0.0:6000
          - run:server
          ports:
          - containerPort: 6000
            name: http
          env:
            - name: DOGE_USERNAME
              valueFrom:
                secretKeyRef:
                  name: dogecoin-rpc
                  key: username
            - name: DOGE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: dogecoin-rpc
                  key: password
            - name: SHKEEPER_BACKEND_KEY
              valueFrom:
                secretKeyRef:
                  name: shkeeper-btc-key
                  key: password
            # ltc_NETWORK
            - name: DOGE_NETWORK
            {{- if .Values.doge_fullnode.mainnet }}
              value: &ltc_NETWORK_VALUE "main"
            {{- else }}
              value: &ltc_NETWORK_VALUE "testnet"
            {{- end }}
            - name: FULLNODE_URL
              value: {{ .Values.doge_fullnode.url }}
            {{- range $name, $value := .Values.dogecoin_shkeeper.extraEnv }}
            - name: {{ $name | quote }}
              value: {{ $value | quote }}
            {{- end }}
          volumeMounts:
            - name: dogecoind-storag
              mountPath: /root/.dogecoin
        - name: tasks
          image: {{ .Values.dogecoin_shkeeper.image }}
          command: ["celery", "-A", "celery_worker.celery", "worker", "--loglevel=info", "-B"]
          env:
            - name: DOGE_USERNAME
              valueFrom:
                secretKeyRef:
                  name: dogecoin-rpc
                  key: username
            - name: DOGE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: dogecoin-rpc
                  key: password
            - name: SHKEEPER_BACKEND_KEY
              valueFrom:
                secretKeyRef:
                  name: shkeeper-btc-key
                  key: password
            - name: ltc_NETWORK
            {{- if .Values.doge_fullnode.mainnet }}
              value: *ltc_NETWORK_VALUE
            {{- else }}
              value: *ltc_NETWORK_VALUE
            {{- end }}
            - name: FULLNODE_URL
              value: {{ .Values.doge_fullnode.url }}
            - name: C_FORCE_ROOT
              value: "1"
            {{- range $name, $value := .Values.dogecoin_shkeeper.extraEnv }}
            - name: {{ $name | quote }}
              value: {{ $value | quote }}
            {{- end }}
          volumeMounts:
            - name: dogecoind-storag
              mountPath: /root/.dogecoin
        - name: redis
          image: redis
      volumes:
      - name: dogecoind-storag
        persistentVolumeClaim:
          claimName: dogecoind-claim
{{- end }}